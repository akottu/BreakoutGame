package breakout;

import javafx.scene.Group;
import javafx.scene.Node;

public class CollisionHandlerTest extends CollisionHandler {

    @Override
    protected void removeChildNode(Group root, Node node) {
        // do nothing for test cases
    }
}
